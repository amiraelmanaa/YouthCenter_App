<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About Us - Arab Youth Centers</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #ffffff;
            color: #1e293b;
            line-height: 1.6;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 24px;
        }

        .wide-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 24px;
        }

         /* Navbar Styles */
        .navbar {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            z-index: 1000;
            padding: 1rem 0;
            transition: all 0.3s ease;
            border-bottom: 1px solid rgba(79, 70, 229, 0.1);
        }

        .navbar.scrolled {
            padding: 0.5rem 0;
            background: rgba(255, 255, 255, 0.98);
            box-shadow: 0 2px 20px rgba(79, 70, 229, 0.1);
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            display: flex;
            align-items: center;
            font-size: 1.8rem;
            font-weight: 700;
            color: #312e81;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .logo:hover {
            transform: scale(1.02);
        }

        .logo-icon {
            width: 50px;
            height: 50px;
            margin-right: 1rem;
            background: linear-gradient(135deg, #4f46e5, #7c3aed);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
            font-size: 1.2rem;
            box-shadow: 0 4px 15px rgba(79, 70, 229, 0.3);
            position: relative;
            overflow: hidden;
        }

        .logo-icon::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent, rgba(255,255,255,0.3), transparent);
            transform: rotate(45deg);
            transition: all 0.6s;
            opacity: 0;
        }

        .logo:hover .logo-icon::before {
            animation: shine 0.6s ease-in-out;
            opacity: 1;
        }

        @keyframes shine {
            0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
            100% { transform: translateX(100%) translateY(100%) rotate(45deg); }
        }

        .nav-menu {
            display: flex;
            list-style: none;
            gap: 2rem;
            align-items: center;
        }

        .nav-link {
            color: #374151;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            position: relative;
            padding: 0.5rem 0;
        }

        .nav-link:hover {
            color: #4f46e5;
        }

        .nav-link::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, #4f46e5, #7c3aed);
            transition: width 0.3s ease;
        }

        .nav-link:hover::after {
            width: 100%;
        }

        .login-btn {
            background: linear-gradient(135deg, #4f46e5, #7c3aed);
            color: white;
            padding: 0.7rem 1.8rem;
            border-radius: 25px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            border: 2px solid transparent;
            box-shadow: 0 4px 15px rgba(79, 70, 229, 0.3);
        }

        .login-btn:hover {
            background: white;
            color: #4f46e5;
            border-color: #4f46e5;
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(79, 70, 229, 0.4);
        }

        .mobile-menu-toggle {
            display: none;
            background: none;
            border: none;
            color: #374151;
            font-size: 1.5rem;
            cursor: pointer;
        }
      /* Hero Section */
.hero {
    position: relative;
    height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    background: linear-gradient(135deg, #210c41ff 0%, #2e1b4dff 25%, #7c3aed 50%, #6b6de6ff 75%, #a855f7 100%);
    background-size: 400% 400%;
    animation: gradientFlow 12s ease infinite;
    overflow: hidden;
    margin-top: 70px;
}

@keyframes gradientFlow {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
}

/* Animated Dots Layer */
.hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: 
        radial-gradient(2px 2px at 20px 30px, rgba(255,255,255,0.3), transparent),
        radial-gradient(2px 2px at 40px 70px, rgba(255,255,255,0.2), transparent),
        radial-gradient(1px 1px at 90px 40px, rgba(255,255,255,0.4), transparent),
        radial-gradient(1px 1px at 130px 80px, rgba(255,255,255,0.2), transparent),
        radial-gradient(2px 2px at 160px 30px, rgba(255,255,255,0.3), transparent),
        radial-gradient(1px 1px at 200px 90px, rgba(255,255,255,0.2), transparent),
        radial-gradient(2px 2px at 240px 50px, rgba(255,255,255,0.4), transparent),
        radial-gradient(1px 1px at 280px 20px, rgba(255,255,255,0.3), transparent),
        radial-gradient(2px 2px at 320px 100px, rgba(255,255,255,0.2), transparent),
        radial-gradient(1px 1px at 360px 60px, rgba(255,255,255,0.3), transparent),
        radial-gradient(2px 2px at 400px 25px, rgba(255,255,255,0.2), transparent),
        radial-gradient(1px 1px at 440px 85px, rgba(255,255,255,0.4), transparent);
    background-repeat: repeat;
    background-size: 500px 150px;
    animation: moveStars 20s linear infinite, pulse 8s ease-in-out infinite;
    pointer-events: none;
}

/* Additional Dot Layer */
.hero::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: 
        radial-gradient(3px 3px at 50px 50px, rgba(255,255,255,0.2), transparent),
        radial-gradient(4px 4px at 100px 100px, rgba(255,255,255,0.15), transparent),
        radial-gradient(3px at 150px 25px, rgba(255,255,255,0.3), transparent),
        radial-gradient(4px 4px at 200px 75px, rgba(255,255,255,0.1), transparent),
        radial-gradient(3px 3px at 250px 125px, rgba(255,255,255,0.25), transparent),
        radial-gradient(4px 4px at 300px 45px, rgba(255,255,255,0.2), transparent),
        radial-gradient(4px 4px at 350px 95px, rgba(255,255,255,0.15), transparent),
        radial-gradient(2px 2px at 400px 15px, rgba(255,255,255,0.3), transparent);
    background-repeat: repeat;
    background-size: 600px 200px;
    animation: moveStars 25s linear infinite reverse, pulse 6s ease-in-out infinite;
    pointer-events: none;
}

@keyframes moveStars {
    0% { transform: translateX(0) translateY(0); }
    100% { transform: translateX(-500px) translateY(-150px); }
}

@keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.7; }
}

.hero-content {
    position: relative;
    z-index: 2;
    max-width: 800px;
    animation: fadeInUp 1s ease-out;
}

.hero h1 {
    font-size: 3.5rem;
    font-weight: 800;
    color: white;
    margin-bottom: 24px;
    text-shadow: 0 4px 20px rgba(0,0,0,0.3);
    line-height: 1.1;
}

.hero p {
    font-size: 1.25rem;
    color: rgba(255,255,255,0.9);
    margin-bottom: 40px;
    font-weight: 400;
}

@keyframes fadeInUp {
    from { opacity: 0; transform: translateY(50px); }
    to { opacity: 1; transform: translateY(0); }
}

        /* Mission Section */
        .mission-section {
            padding: 120px 0;
            background: #f8fafc;
            position: relative;
        }

        .mission-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 80px;
            align-items: center;
        }

        .mission-text h2 {
            font-size: 2.5rem;
            font-weight: 800;
            color: #0f172a;
            margin-bottom: 24px;
            line-height: 1.2;
        }

        .mission-text p {
            font-size: 1.125rem;
            color: #475569;
            margin-bottom: 24px;
            font-weight: 400;
        }

        .mission-visual {
            position: relative;
            border-radius: 24px;
            overflow: hidden;
            box-shadow: 0 25px 50px rgba(30, 58, 138, 0.15);
            transform: rotate(2deg);
            transition: transform 0.5s ease;
        }

        .mission-visual:hover {
            transform: rotate(0deg) scale(1.05);
        }

        .mission-visual img {
            width: 100%;
            height: 400px;
            object-fit: cover;
        }

        .mission-placeholder {
            width: 100%;
            height: 400px;
            background: linear-gradient(135deg, #e2e8f0 0%, #cbd5e1 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: #64748b;
            font-size: 1.1rem;
            font-weight: 600;
        }

        /* Values Section */
        .values-section {
            padding: 120px 0;
            background: white;
        }

        .section-header {
            text-align: center;
            margin-bottom: 80px;
        }

        .section-header h2 {
            font-size: 2.5rem;
            font-weight: 800;
            color: #0f172a;
            margin-bottom: 16px;
        }

        .section-header p {
            font-size: 1.125rem;
            color: #64748b;
            max-width: 600px;
            margin: 0 auto;
        }

        .values-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
        }

        .value-card {
            text-align: center;
            padding: 40px 32px;
            border-radius: 20px;
            background: white;
            box-shadow: 0 8px 30px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
            border: 1px solid #f1f5f9;
            position: relative;
            overflow: hidden;
        }

        .value-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #1e3a8a, #3b82f6);
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }

        .value-card:hover::before {
            transform: scaleX(1);
        }

        .value-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(30, 58, 138, 0.15);
        }

        .value-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto 24px;
            background: linear-gradient(135deg, #eff6ff, #dbeafe);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #1e40af;
        }

        .value-card h3 {
            font-size: 1.5rem;
            font-weight: 700;
            color: #1e3a8a;
            margin-bottom: 16px;
        }

        .value-card p {
            color: #64748b;
            font-size: 1rem;
            line-height: 1.6;
        }

        /* Stats Section */
        .stats-section {
            padding: 120px 0;
            background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%);
            color: white;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .stats-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="25" cy="25" r="1" fill="white" opacity="0.1"/><circle cx="75" cy="50" r="1.5" fill="white" opacity="0.1"/><circle cx="50" cy="75" r="1" fill="white" opacity="0.1"/></svg>');
        }

        .stats-content {
            position: relative;
            z-index: 2;
        }

        .stats-header {
            margin-bottom: 80px;
        }

        .stats-header h2 {
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 16px;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 60px;
        }

        .stat-item {
            position: relative;
        }

        .stat-number {
            font-size: 3rem;
            font-weight: 900;
            margin-bottom: 8px;
            background: linear-gradient(135deg, #ffffff, #e2e8f0);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .stat-label {
            font-size: 1.125rem;
            font-weight: 600;
            opacity: 0.9;
        }

        /* Team Section */
        .team-section {
            padding: 120px 0;
            background: #f8fafc;
        }

        .team-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 40px;
            margin-top: 80px;
        }

        .team-card {
            background: white;
            border-radius: 20px;
            padding: 32px;
            text-align: center;
            box-shadow: 0 8px 30px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
            border: 1px solid #f1f5f9;
        }

        .team-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(30, 58, 138, 0.15);
        }

        .team-avatar {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            margin: 0 auto 24px;
            object-fit: cover;
            border: 4px solid #eff6ff;
        }

        .avatar-placeholder {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            margin: 0 auto 24px;
            background: linear-gradient(135deg, #e2e8f0, #cbd5e1);
            display: flex;
            align-items: center;
            justify-content: center;
            color: #64748b;
            border: 4px solid #eff6ff;
        }

        .team-card h3 {
            font-size: 1.25rem;
            font-weight: 700;
            color: #1e3a8a;
            margin-bottom: 8px;
        }

        .team-role {
            color: #64748b;
            font-weight: 500;
            margin-bottom: 16px;
        }

        .team-description {
            color: #475569;
            font-size: 0.95rem;
            line-height: 1.6;
        }

        /* CTA Section */
        .cta-section {
            padding: 120px 0;
            background: white;
            text-align: center;
        }

        .cta-content {
            max-width: 600px;
            margin: 0 auto;
        }

        .cta-content h2 {
            font-size: 2.5rem;
            font-weight: 800;
            color: #0f172a;
            margin-bottom: 24px;
        }

        .cta-content p {
            font-size: 1.125rem;
            color: #64748b;
            margin-bottom: 40px;
        }

        .cta-buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            padding: 16px 32px;
            border-radius: 12px;
            font-weight: 600;
            font-size: 1rem;
            text-decoration: none;
            transition: all 0.3s ease;
            cursor: pointer;
            border: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .btn-primary {
            background: linear-gradient(135deg, #1e3a8a, #1e40af);
            color: white;
            box-shadow: 0 8px 25px rgba(30, 58, 138, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 35px rgba(30, 58, 138, 0.4);
        }

        .btn-secondary {
            background: white;
            color: #1e3a8a;
            border: 2px solid #1e3a8a;
        }

        .btn-secondary:hover {
            background: #1e3a8a;
            color: white;
            transform: translateY(-2px);
        }

        /* Footer */
        .footer {
            background: #0f172a;
            color: #e2e8f0;
            padding: 80px 0 40px;
        }

        .footer-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
            margin-bottom: 40px;
        }

        .footer-section h3 {
            font-size: 1.25rem;
            font-weight: 700;
            margin-bottom: 20px;
            color: white;
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 12px;
        }

        .footer-links a {
            color: #94a3b8;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .footer-links a:hover {
            color: #3b82f6;
        }

        .footer-bottom {
            border-top: 1px solid #334155;
            padding-top: 40px;
            text-align: center;
            color: #64748b;
        }

        /* Responsive Design */
        @media (max-width: 968px) {
            .nav-links {
                display: none;
            }

            .hero h1 {
                font-size: 2.5rem;
            }

            .mission-content {
                grid-template-columns: 1fr;
                gap: 60px;
            }

            .mission-visual {
                transform: rotate(0deg);
            }

            .cta-buttons {
                flex-direction: column;
                align-items: center;
            }
        }

        @media (max-width: 640px) {
            .container, .wide-container {
                padding: 0 16px;
            }

            .hero {
                height: 70vh;
                padding: 0 20px;
            }

            .hero h1 {
                font-size: 2rem;
            }

            .hero p {
                font-size: 1.1rem;
            }

            .mission-section,
            .values-section,
            .stats-section,
            .team-section,
            .cta-section {
                padding: 80px 0;
            }

            .values-grid,
            .team-grid {
                grid-template-columns: 1fr;
            }

            .stats-grid {
                grid-template-columns: repeat(2, 1fr);
                gap: 40px;
            }
        }
        
    </style>
</head>
<body>

<!-- Navigation -->
    <nav class="navbar" id="navbar">
        <div class="nav-container">
            <a href="#" class="logo">
                <div class="logo-icon">YC</div>
                YouthConnect
            </a>
            <ul class="nav-menu" id="nav-menu">
                <li><a href="{{ path('home') }}" class="nav-link">Home</a></li>
                
                <li><a href="{{ path('app_about') }}" class="nav-link">About</a></li>
               
                <li>
                {% if app.user %}
                    <a href="{{ path('app_user_bookings_journal') }}" class="nav-link">My Bookings</a>
                {% else %}
                    <a href="#" 
                       class="nav-link" 
                       onclick="alert('Please log in first to access your bookings.'); return false;">
                        My Bookings
                    </a>
                {% endif %}
            </li>
                <li>
                {% if app.user %}
                 <a href="{{ path('app_logout') }}" class="login-btn">Logout</a>
                 {% else %}
                  <a href="{{ path('app_register') }}" class="login-btn">Login</a>
                  {% endif %}
                </li>
            </ul>
            <button class="mobile-menu-toggle" id="mobile-menu-toggle">☰</button>
        </div>
    </nav>
   

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <h1>Empowering Arab Youth Across Communities</h1>
            <p>Building bridges, fostering connections, and creating opportunities for young people throughout the Arab world to grow, learn, and thrive together.</p>
        </div>
    </section>

    <!-- Mission Section -->
    <section class="mission-section">
        <div class="container">
            <div class="mission-content">
                <div class="mission-text">
                    <h2>Our Mission</h2>
                    <p>We believe every young person deserves access to spaces where they can discover their potential, build meaningful relationships, and contribute to their communities.</p>
                    <p>Arab Youth Centers serves as a comprehensive platform connecting young people with community spaces, programs, and opportunities that align with their interests, goals, and cultural values.</p>
                    <p>Through our network of carefully curated youth centers, we're building a stronger, more connected Arab youth community that transcends geographical boundaries.</p>
                </div>
                <div class="mission-visual">
                    {% if mission_image %}
                        <img src="{{ mission_image }}" alt="Young people collaborating">
                    {% else %}
                        <div class="mission-placeholder">
                            <svg width="80" height="80" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197m13.5-9a2.5 2.5 0 11-5 0 2.5 2.5 0 015 0z"></path>
                            </svg>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </section>

    <!-- Values Section -->
    <section class="values-section">
        <div class="container">
            <div class="section-header">
                <h2>Our Core Values</h2>
                <p>The principles that guide our work and define our commitment to Arab youth development</p>
            </div>
            <div class="values-grid">
                <div class="value-card">
                    <div class="value-icon">
                        <svg width="40" height="40" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
                        </svg>
                    </div>
                    <h3>Community</h3>
                    <p>We foster inclusive environments where young people from diverse backgrounds come together to learn, grow, and support one another.</p>
                </div>
                <div class="value-card">
                    <div class="value-icon">
                        <svg width="40" height="40" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                        </svg>
                    </div>
                    <h3>Empowerment</h3>
                    <p>We provide resources, opportunities, and platforms that enable young people to discover their strengths and pursue their passions.</p>
                </div>
                <div class="value-card">
                    <div class="value-icon">
                        <svg width="40" height="40" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                    </div>
                    <h3>Cultural Pride</h3>
                    <p>We celebrate Arab heritage and identity while encouraging young people to embrace their roots and contribute to their cultural legacy.</p>
                </div>
                <div class="value-card">
                    <div class="value-icon">
                        <svg width="40" height="40" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
                        </svg>
                    </div>
                    <h3>Innovation</h3>
                    <p>We embrace modern approaches and creative solutions to address the evolving needs and aspirations of today's Arab youth.</p>
                </div>
                <div class="value-card">
                    <div class="value-icon">
                        <svg width="40" height="40" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
                        </svg>
                    </div>
                    <h3>Excellence</h3>
                    <p>We maintain the highest standards in our programs, partnerships, and services to ensure meaningful impact and lasting value.</p>
                </div>
                <div class="value-card">
                    <div class="value-icon">
                        <svg width="40" height="40" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
                        </svg>
                    </div>
                    <h3>Inclusivity</h3>
                    <p>We welcome young people from all walks of life, creating spaces where diversity is celebrated and everyone feels valued and heard.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Stats Section -->
    <section class="stats-section">
        <div class="container">
            <div class="stats-content">
                <div class="stats-header">
                    <h2>Impact Across the Arab World</h2>
                    <p>Connecting communities, transforming lives, and building a brighter future for Arab youth</p>
                </div>
                <div class="stats-grid">
                    <div class="stat-item">
                        <div class="stat-number">{{ stats.centers ?? '50+' }}</div>
                        <div class="stat-label">Youth Centers</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-number">{{ stats.countries ?? '15' }}</div>
                        <div class="stat-label">Arab Countries</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-number">{{ stats.youth_served ?? '10K+' }}</div>
                        <div class="stat-label">Youth Served</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-number">{{ stats.programs ?? '200+' }}</div>
                        <div class="stat-label">Active Programs</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Team Section -->
    <section class="team-section">
        <div class="container">
            <div class="section-header">
                <h2>Meet Our Team</h2>
                <p>Passionate professionals dedicated to empowering Arab youth and strengthening communities</p>
            </div>
            <div class="team-grid">
                {% for member in team_members|default([
    {name: 'Ahmad Al-Rahman', role: 'Executive Director', description: 'Passionate advocate for youth development with over 15 years of experience in community building across the MENA region.', image: '/images/default-profile.jpg'},
    {name: 'Fatima Kassem', role: 'Program Director', description: 'Educational specialist focused on creating innovative programs that bridge traditional values with modern learning approaches.', image: '/images/default-profile.jpg'},
    {name: 'Omar Hakim', role: 'Technology Lead', description: 'Tech entrepreneur dedicated to leveraging digital platforms to connect and empower Arab youth communities worldwide.', image: '/images/default-profile.jpg'},
    {name: 'Layla Mansour', role: 'Community Partnerships', description: 'Relationship builder with deep connections across Arab communities, fostering collaboration and shared growth.', image: '/images/default-profile.jpg'}
]) %}
                    <div class="team-card">
                        {% if member.image %}
                           <img src="{{ member.image|default('/images/default-profile.jpg') }}" alt="{{ member.name }}">
                        {% else %}
                            <div class="avatar-placeholder">
                                <svg width="40" height="40" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                                </svg>
                            </div>
                        {% endif %}
                        <h3>{{ member.name }}</h3>
                        <div class="team-role">{{ member.role }}</div>
                        <p class="team-description">{{ member.description }}</p>
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
        <div class="container">
            <div class="cta-content">
                <h2>Join